name: "Build and publish all devcontainers to Dockerhub"

# triggers
on:
  workflow_dispatch:
  # push:
  # pull_request:

jobs:
  buildanddeploy:
    name: "For each"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: "Docker Build and Publish"
        run: |
          find . -maxdepth 1 -type d \( ! -name ".*" \) -exec bash -c 'echo "$0"'  \;
