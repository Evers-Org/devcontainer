name: "Build and publish all devcontainers to Dockerhub"

# triggers
on:
  workflow_dispatch:
  # push:
  # pull_request:

jobs:
  buildanddeploy:
    name: "For each"
    uses: ./.github/workflows/single.yaml
    with:
      image: svelte
